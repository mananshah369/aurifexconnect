# aurifexconnect

# 📊 ERP System

This is a simple **ERP (Enterprise Resource Planning)** system built using **Spring Boot**. The goal of this project is to provide a backend system for managing typical ERP functionalities like inventory, employee records, customer data, etc.

---

## 🛠️ Technologies Used

- **Java 21**
- **Spring Boot 3.4.4**
- **Spring Data JPA**
- **SQLite Database**
- **Maven** (for project management and build)
- **Lombok** (for cleaner code with annotations)
- **MapStruct** (for object mapping)
- **Swagger (Springdoc OpenAPI)** (for API documentation)
- **JUnit & Mockito** (for unit testing)

---

## 🚀 How to Run the Project

Follow these steps to set up and run the project locally:

### 1. Clone the Repository

```bash
git clone https://github.com/your-username/ERP.git
cd ERP


 1. Clone the Repository

```bash
mvn clean install

 3. Run the Application

```bash
git clone https://github.com/your-username/ERP.git

cd ERP